import MainBanner from "../components/mainBanner";
import Layout from "../layout";

const Home = () => {
  return (
    <Layout location={location}>
      <MainBanner />
    </Layout>
  );
};

export default Home;
